name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    env:
      PORT: 8000
      DB_NAME: indexer
      DB_SECRET: qwert
      DJANGO_SECRET: "FDJDSLJFHUDHJCTKLLLCMNII(****#TEFF"
      HOST: localhost
      API_SECRET: "test"
      SERVICE_NAME: "IETF BibXML service"
      CONTACT_EMAIL: "test@email.com"
      DEBUG: 1
      SNAPSHOT: "test"
    steps:
      - uses: actions/checkout@v3
      - name: Build the Docker image and start the environment
        run: docker compose -f docker-compose.test.yml run
#      - name: Run tests
#        run: docker compose run test python manage.py test main.tests.test_query
      - name: Dump docker logs on failure
        if: always()
        uses: jwalton/gh-docker-logs@v1
      - name: Stop Containers
        if: always()
        run: docker-compose down
